// Place your settings in this file to overwrite default and user settings.
{
    // Exclude files for typscript dev
    "files.exclude": {
        "**/*.js": {
            "when": "$(basename).ts"
        },
        "**/*.js.map": {
            "when": "$(basename)"
        }
    },
    "files.trimTrailingWhitespace": true
}